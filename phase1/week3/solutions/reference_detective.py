"""Mini-project — reference-detective.

A small CLI tool that answers the question behind a whole family of tickets:
"I only touched one thing — why did something else change?" Given a few names
you bind to objects, it x-rays how Python actually holds them:

  Section 1 — Names & identity: for each name, print its id and refcount, and
  group the names that are aliases of the same object (same id) versus ones
  that are merely equal but separate.

  Section 2 — Mutation blast radius: mutate one shared object once and report
  every name whose value changed as a side effect — the aliasing blast radius
  made visible.

  Section 3 — Copy comparison: take a nested structure, build a shallow copy
  and a deep copy, mutate a nested element in the original, and show which
  copy still shares it (changed) and which isolates it (unchanged).

This is the third artifact in your GitHub portfolio. See the Phase 1 README
(Graduation Projects) for the expected output format.
"""

# TODO: implement the mini-project.
print("reference-detective — not implemented yet. Build your name/reference inspector here!")

print("     :)") 
