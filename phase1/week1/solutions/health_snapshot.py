"""Mini-project — health-snapshot.

A small CLI tool that does two things a support engineer does in the first
minute of triaging a slow machine:

  Section 1 — This machine: print CPU cores, total/free RAM, free disk
  space, and the OS (using psutil, platform, and shutil).

  Section 2 — Where time goes: time an in-memory loop, a small disk read,
  and one real network request, then print a table with the average time per
  operation and a "slowdown vs the fastest" column.

This is the first artifact in your GitHub portfolio. See the week's README
for the expected output format.
"""

# TODO: implement the mini-project.
print("health-snapshot — not implemented yet. Build your machine report + speed demo here!")
